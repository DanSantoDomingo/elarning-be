version: '3'

services:
  web:
    build:
      context: .
      dockerfile: ./docker/Dockerfile
    container_name: django-be
    command: /bin/sh -c "docker/start_django.sh"
    volumes:
      - .:/code
    ports:
      - "8000:8000"
